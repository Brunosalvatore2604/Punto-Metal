{
  "name": "PuntoMetal",
  "type": "nodejs",
  "buildCommand": "npm install",
  "startCommand": "node server.js",
  "env": {
    "PORT": "${PORT}"
  },
  "healthCheck": {
    "path": "/health",
    "interval": 30
  }
}
